
/**
 * isEmpty returns true if the passed array
 * is empty (does not have a length greater than 0) and
 * false if the array is NOT empty (has a length greater than 0).
 * @param {Array} array 
 */
function isEmpty(array){
    return ! array.length;
}
exports.isEmpty = isEmpty;